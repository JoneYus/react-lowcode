import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;
const LayoutFooter = () => {
  return <Footer style={{ textAlign: 'center', background: '#FFF' }}>React Router Demo @2022</Footer>;
};

export default LayoutFooter;
